FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends openvpn wireguard-tools iproute2 iptables openssl python3 curl tcpdump ca-certificates && rm -rf /var/lib/apt/lists/*
CMD ["sleep","infinity"]
